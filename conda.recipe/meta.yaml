package:
  name: gdx-reader
  version: "0.2.2"
 
source:
  git_url: ../
  git_tag: master

requirements:
  build:
    - python
    - cython
    - numpy ==1.10.1
  run:
    - python
    - numpy ==1.10.1
    - pandas
 
about:
  summary: "Read GDX files fast using the GAMS C API"